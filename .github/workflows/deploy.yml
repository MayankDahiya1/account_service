name: Deploy Account Service

on:
  push:
    branches: [main]

jobs:
  build-deploy:
    runs-on: self-hosted
    environment: production
    steps:
      - uses: actions/checkout@v3

      - name: Log in to DockerHub
        run: echo "${{ secrets.DOCKERHUB_PASSWORD }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/account_service:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/account_service:latest

      - name: Restart container on server
        run: |
          if [ "$(docker ps -q -f name=account_service)" ]; then
            echo "🔄 Restarting existing container..."
            docker stop account_service
            docker rm account_service
          else
            echo "🚀 No existing container, creating new one..."
          fi

          docker run -d --name account_service \
            --network microservices-infra_default \    # 🔹 Auto connect network
            -e DATABASE_URL=${{ secrets.DATABASE_URL }} \
            -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
            -e REFRESH_SECRET=${{ secrets.REFRESH_SECRET }} \
            -e NODE_ENV=${{ secrets.NODE_ENV }} \
            -e KAFKA_BROKER=kafka:9092 \               # 🔹 Kafka container name use karo
            -e GATEWAY_URL=${{ secrets.GATEWAY_URL }} \
            -e REDIS_HOST=redis \                      # 🔹 Redis container name use karo
            -e REDIS_PORT=6379 \                       # 🔹 Direct port use karo
            -e PORT=${{ secrets.PORT }} \
            -p ${{ secrets.PORT }}:${{ secrets.PORT }} \
            ${{ secrets.DOCKERHUB_USERNAME }}/account_service:latest
